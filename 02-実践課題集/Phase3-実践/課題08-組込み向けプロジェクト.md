# 課題08: 組込み向けプロジェクト

[[README|← トップに戻る]]  |  [[課題09-CLIツール|次の課題 →]]

**難易度:** ⭐️⭐️⭐️⭐️
**学習時間目安:** 10-15時間
**学習トピック:** no_std環境、embedded-hal、組込みRust

---

## 🎯 課題の目的

組込み環境でのRust開発を学び、実際のハードウェアでプログラムを動かします。

---

## 📋 対象ハードウェア

### 推奨: Raspberry Pi Pico (RP2040)

- 安価（約600円）
- Rust対応が充実
- デバッグが簡単

### 代替案
- STM32 Nucleo
- ESP32-C3
- BBC micro:bit v2

---

## 🏗️ プロジェクト例

### プロジェクト1: Lチカ（LED点滅）

```rust
#![no_std]
#![no_main]

use panic_halt as _;
use rp2040_hal as hal;

#[link_section = ".boot2"]
#[used]
pub static BOOT2: [u8; 256] = rp2040_boot2::BOOT_LOADER_GENERIC_03H;

#[hal::entry]
fn main() -> ! {
    let mut pac = hal::pac::Peripherals::take().unwrap();
    let mut watchdog = hal::Watchdog::new(pac.WATCHDOG);

    let clocks = hal::clocks::init_clocks_and_plls(
        // ... クロック設定
    ).ok().unwrap();

    let sio = hal::Sio::new(pac.SIO);
    let pins = hal::gpio::Pins::new(/* ... */);

    let mut led_pin = pins.gpio25.into_push_pull_output();

    loop {
        led_pin.set_high().unwrap();
        cortex_m::asm::delay(1_000_000);
        led_pin.set_low().unwrap();
        cortex_m::asm::delay(1_000_000);
    }
}
```

### プロジェクト2: 温度センサーの読み取り

### プロジェクト3: UARTシリアル通信

---

## 💡 no_std環境の理解

### 標準ライブラリとの違い

```rust
// std環境（PC）
use std::collections::HashMap;
use std::fs::File;
use std::thread;

// no_std環境（組込み）
#![no_std]
// HashMapやFileは使えない
// スレッドも使えない
// ヒープアロケーションも限定的
```

### 組込み特有の考慮事項

1. **メモリ制約**
   - スタックサイズが小さい
   - ヒープが使えないことも

2. **リアルタイム性**
   - 割り込みハンドラ
   - タイミングクリティカルな処理

3. **ペリフェラル制御**
   - GPIO、UART、SPI、I2Cなど

---

## 🚀 発展課題

### レベル1: PWMでのLED調光

### レベル2: I2Cセンサーとの通信

### レベル3: RTICフレームワークでのリアルタイムOS

---

## 🔗 関連資料

- [The Embedded Rust Book](https://docs.rust-embedded.org/book/)
- [Awesome Embedded Rust](https://github.com/rust-embedded/awesome-embedded-rust)

---

## 🎓 完了チェックリスト

- [ ] no_std環境でのコンパイルができる
- [ ] ハードウェアでコードが動作する
- [ ] embedded-halの使い方を理解した

---

[[課題09-CLIツール|次の課題: CLIツール →]]
